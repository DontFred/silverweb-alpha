import { Fragment } from 'react'
import { ArrayProps } from '../types'

export default function ArrayField(props: ArrayProps & {name: string}) {

    const { name, option, item, helpText } = props;

  return (
    <Fragment>

    </Fragment>
  )
}
